# ─────────────────────────────────────────────
#  Copia este archivo como .env y completa
#  los valores reales. NUNCA subas .env a Git.
# ─────────────────────────────────────────────

# ── Telegram ──────────────────────────────────
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# ── Groq (LLM principal: NLP + Whisper + Vision) ──
# Obtené tu clave en: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile   # modelo de texto
# GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct  # alternativa con visión

# ── OpenAI (clave requerida por config.py, no se usa activamente) ──
OPENAI_API_KEY=sk-fake-or-real-openai-key
OPENAI_MODEL=gpt-4o-mini

# ── Supabase (base de datos) ──────────────────
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_KEY=your_supabase_anon_or_service_key

# ── Encriptación ──────────────────────────────
# Generá una clave nueva con:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANTE: si perdés esta clave, los datos en Supabase son irrecuperables.
# Guardala en un lugar seguro (gestor de contraseñas, etc.)
ENCRYPTION_KEY=your_fernet_encryption_key_here

# ── Entorno ───────────────────────────────────
ENV=production     # development | production
LOG_LEVEL=INFO
